# Guard Management System

Modular Guard Management System foundation with a Django REST API, React admin dashboard, React client portal, and an isolated patrol device SDK integration layer.

See `PROJECT_PLAN.md` for the architecture and roadmap.
